============
Installation
============

At the command line::

    $ easy_install dj-facebook

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dj-facebook
    $ pip install dj-facebook
